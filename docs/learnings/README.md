# docs/learnings/

**Accumulated knowledge layer. Contents are gitignored; only this README is committed.**

This is the project's persistent research memory — technical notes that took effort to discover and shouldn't be rediscovered: hazard-model status, component-inventory findings, EDP/recovery calibration notes, reconciliation analyses. Agents read it before making assumptions and write to it when they learn something non-obvious (see [`agentic-harness-principles.md`](../agentic-harness-principles.md), §8).

Kept private as a working layer: clone this repo and the folder is empty by design. Only this marker is committed, so the convention is *visible* without exposing the in-progress notes.

**Rule:** keep these notes strictly technical — no strategy, personal context, or anything sensitive. Those belong in `.local/`.
