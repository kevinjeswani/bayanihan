# sandbox/

**Scratch space. Contents gitignored; only this README is committed.**

Throwaway experiments, one-off pokes, quick data checks — things that aren't part of the package and don't belong in `prototypes/` (which is a curated, dated record of the build). Nothing here is imported by the package or referenced by tests.

Gitignored by design: clone this repo and `sandbox/` is empty.
